Syntax:
  SELECT Col_name FROM table_name
  WHERE Col_name Condition ALL(
    SELECT Col_name FROM table_name
    WHERE Condition 
  );

Example:
  SELECT * FROM Sales 
  WHERE amount > ALL(
    SELECT amount FROM Sales 
    WHERE region = "south"
  );